// const mongoose_app = require('mongoose');

// mongoose_app.connect(process.env.DATABASE_URL);

// const db = mongoose_app.connection;

// db.on('connected', function () {
//   console.log(`Connected to ${db.name} at ${db.host}:${db.port}`);
// });